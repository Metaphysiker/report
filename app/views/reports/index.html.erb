<p id="notice"><%= notice %></p>

<h1>Reports</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reports.each do |report| %>
      <tr>
        <td><%= report.date %></td>
        <td><%= link_to 'Show', report %></td>
        <td><%= link_to 'Edit', edit_report_path(report) %></td>
        <td><%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Report', new_report_path %>

<h1>Benutzerprofile erstellt, nur Mitglieder)</h1>
<%= line_chart AlchemyUser.where(alchemy_roles: "member").group_by_week(:created_at).count %>

<h1>Kommentare erstellt</h1>
<%= line_chart Comment.group_by_week(:created_at).count %>


<h1>Interessensgebiete, nur Mitglieder</h1>
<%= bar_chart @interests, height: "1500px"%>


<h1>Reports</h1>
<%= line_chart @memberinos %>

